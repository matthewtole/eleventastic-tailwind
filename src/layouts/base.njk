<!DOCTYPE html>
<html lang="{{ meta.lang }}" class="box-border">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ meta.title }}</title>

    <link rel="stylesheet" href="{{ '/assets/styles/main.css' | url }}">
    {% include "meta.njk" %}
</head>
<body>
    <div hidden>{% iconsprite %}</div>
    <a href="#main" class="absolute w-1 h-1 focus:h-auto overflow-hidden focus:w-auto focus:t-0 focus:px-6 focus:py-4 focus:text-white focus:bg-gray-700 rounded-b-lg focus:left-1/2 transform focus:-translate-x-2/4 text-base underline">skip to main content</a>

    <div class="flex min-h-screen flex-col relative" role="document">
        {% include "header.njk" %}
        <main id="main" class="flex flex-col flex-grow" tabindex="-1">
            {{ content | safe }}
        </main>
        {% include "footer.njk" %}
    </div>

    <script type="text/javascript" src="{{ '/assets/scripts/main.js' | url }}"></script>
    <script type="text/javascript">
        if ('serviceWorker' in navigator) {
            navigator.serviceWorker.register('/serviceworker.js')
        }
    </script>
</body>
</html>